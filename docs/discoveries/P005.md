## Discovery Report — P005

### Assumptions trong phiếu — ĐÚNG:

- Anchor #1: `src/cli/status.rs:15` — `bail!("\`status\` not yet implemented (Phase 1.5)")`. Exact stub line confirmed.
- Anchor #2: `src/cli/mod.rs:25` — `Status(status::Args)` newtype dispatch confirmed. `dispatch()` at line 34. Zero `mod.rs` edit required.
- Anchor #3 (V2 corrected form): `fn bootstrap(&self, plist_path: &Path) -> Result<()>` at `src/launchd.rs:154` — 1-arg form confirmed (domain computed internally). `fn bootout(&self, label: &str) -> Result<()>` at line 161 confirmed.
- Anchor #4: `pub fn current_uid() -> Result<u32>` at `src/launchd.rs:238` confirmed.
- Anchor #5: `pub struct RealLaunchctl;` at `src/launchd.rs:165` — unit struct, instantiated as `RealLaunchctl` (no `::new()`).
- Anchor #6: `pub struct NoopLaunchctl { ... }` at `src/launchd.rs:216` — `#[derive(Default)]`, instantiated as `NoopLaunchctl::default()` (not `::new()`).
- Anchor #7: `heartbeat::read_last_n(log_path: &Path, n: usize) -> Result<Vec<HeartbeatRecord>>` — returns Vec oldest-first; empty Vec on missing file.
- Anchors #8–#15: All config/Cargo.toml deps confirmed (serde_json, chrono, tempfile, etc.).
- Anchor #26: Pre-P005 max INV = INV-16. INV-17 slot confirmed available and now assigned.
- Anchor #27: Baseline test count = 51 (34 unit + 3 cli_help + 4 cli_init + 6 cli_register + 4 cli_run). Confirmed exact.
- Anchor #29: `#[derive(clap::Args)]` pattern confirmed for all per-subcommand Args structs.
- Anchor #30: `stdout_tail` / `stderr_tail` are `String` fields in `HeartbeatRecord`. `tail_first_n_or_empty` converts empty → `(empty)` in human render.
- Anchor #31 (V2 empirical, Turn 1): macOS 15 (Darwin 25.5.0) `launchctl print` for `StartCalendarInterval` jobs outputs `descriptor = { "Minute" => M "Hour" => H }` in `event triggers` block. NO "next fire" / "next launch" / "run at" timestamp key present. `parse_next_fire` implemented to parse these two keys → "daily at HH:MM".

### Assumptions trong phiếu — SAI so với code thật:

- **Anchor #3 V1 form** (phiếu V1 Task 1 code block showed `fn bootstrap(&self, domain: &str, plist_path: &Path) -> Result<()>` — 2-arg form). Real code at `src/launchd.rs:154` is 1-arg: `fn bootstrap(&self, plist_path: &Path) -> Result<()>` (domain computed internally via `current_uid()`). V2 corrected this in Task 1 code block per Debate Log Turn 1 [O1.1]. Worker preserved actual signature throughout EXECUTE.

- **Anchor #28 V1 strategy** (phiếu V1 specced 4 timestamp-key candidates: "next fire = ...", "next launch = ...", etc.). Real macOS 15 launchctl output contains NONE of these for `StartCalendarInterval` jobs. The only schedule data is `descriptor = { "Minute" => 0 "Hour" => 9 }` nested in `event triggers`. V2 pivoted `parse_next_fire` to extract Hour/Minute from descriptor block → "daily at HH:MM". This is the highest-value finding: satisfies Phase 1 acceptance gate ("shows next fire") via "shows configured recurrence" — honest framing, not a timestamp guess.

### Edge cases / limitations phát hiện thêm:

- **`cli_help.rs` regression check**: `cargo test --test cli_help` ran 3/3 pass without any update needed. The existing test `each_subcommand_help_exits_zero` exercises all subcommands' `--help` — it passed with new Args flags, confirming clap derive propagation is automatic via newtype dispatch.

- **NoopLaunchctl `print_calls` tracking**: The phiếu mentioned "Worker may store last call args via interior mutability" for `NoopLaunchctl::print`. Decision: NOT added. Integration tests verify status behavior end-to-end via binary spawn (not via `NoopLaunchctl` directly in integration tests). `RealLaunchctl::print` is exercised in unit tests (reject invalid/empty label). `NoopLaunchctl::print` is used in unit tests to verify canned output format. No test needs to assert `NoopLaunchctl` was called with a specific label — the integration tests use binary spawn with `--label definitely-not-loaded-label-p005` which exercises the real `RealLaunchctl::print` path (returning `not_loaded=true`). No interior mutability needed. Tầng 2 self-decide: simpler code, full coverage still achieved.

- **Clippy collapsible-if** in `parse_next_fire`: nested `if let` + `if <range_check>` blocks flagged by clippy as collapsible. Fixed using Rust edition 2024 `if let Ok(h) = ... && h < 24 { }` pattern (let-chain syntax). This is a stylistic Tầng 2 self-correct — the logic is identical.

- **`parse_next_fire` whitespace handling**: The macOS 15 launchctl output uses tab-indentation before `"Hour"` and `"Minute"` keys. `line.trim()` on each line before matching handles this correctly for both the real launchctl output and the MACOS15_FIXTURE string literal. The fixture uses `\t` escape sequences embedded in a string continuation — `line.trim()` correctly strips leading tabs.

- **`read_last_n` oldest-first ordering**: Confirmed by P004 contract. Human render reverses to newest-first (`heartbeats.iter().rev()`). JSON mode preserves oldest-first (machine consumers can sort). Test `status_last_flag_clamps_heartbeat_count` writes 5 heartbeats, requests 3, counts `exit=` occurrences = exactly 3. Correct.

- **`serde_json` in integration tests**: `serde_json::from_str` available in `tests/cli_status.rs` because `serde_json` is a `[dependencies]` entry (not just `[dev-dependencies]`) — available everywhere.

### Docs đã cập nhật theo discoveries:

- `docs/ARCHITECTURE.md`:
  - §CLI surface `status` row Args column: updated from `--json (machine output)` to full flags `--label / --config / --json / --last`.
  - §Modules table `src/cli/status.rs` row: Phase column updated from `1.1 skeleton ✅ → impl 1.5` to `1.5 ✅`.
  - §Modules table `src/launchd.rs` row: appended note about `LaunchctlClient::print` + `LaunchctlPrintOutput` + descriptor parser.
  - §Cron mechanism `status` line: updated to note macOS 15 descriptor-only output + `parse_next_fire` strategy.
  - §Phase status: updated to mark Phase 1.5 shipped with full description + macOS 15 discovery note.
- `docs/CHANGELOG.md`: P005 entry added at top with full details (CLI flags, trait extension, descriptor-parser pivot, INV-17, test count).
- `docs/security/INVARIANTS.md`: INV-17 appended (launchctl print shell-out boundary — label sanitization + discrete arg passing). INV count: 16 → 17.
- `docs/DISCOVERIES.md`: 1-line index entry added at top.

### Verification Trace results:

| Sub-mech | Result |
|----------|--------|
| A (trigger) | N/A — manual test required (real loaded plist). Verified: `definitely-not-loaded-label-p005` → "not loaded" output confirmed via integration test. |
| B (capability) | `cargo check` ✅ clean. `cargo test --all` ✅ 70 pass (48+3+4+6+4+5). |
| C (migration) | N/A — no schema change. |
| D (persistence) | `grep -l "status" docs/ARCHITECTURE.md` ✅ hit. `grep -c "^### INV-" docs/security/INVARIANTS.md` = 17 > 16 (pre-P005). ✅ |
| E (env drift) | `cargo update --dry-run` → only displaydoc 0.2.5→0.2.6 patch bump (no major). ✅. `cargo build --release` ✅ 1.2MB binary. |
| mod.rs constraint | `git diff src/cli/mod.rs` = empty. ✅ |
| Cargo.toml constraint | `git diff Cargo.toml` = empty. ✅ |

### Post-P005 test count:

51 baseline + 19 new = 70 total:
- 48 unit tests in binary crate (was 34; +14 from status.rs unit tests + launchd.rs print tests)
- 3 cli_help (unchanged)
- 4 cli_init (unchanged)
- 6 cli_register (unchanged)
- 4 cli_run (unchanged)
- 5 cli_status (new — P005)

### Key finding for P006 (MCP wrapper):

`status` command renders identically in `--json` mode regardless of whether plist is loaded. JSON output shape:

```json
{
  "label": "...",
  "plist_loaded": false,
  "next_fire": null,
  "heartbeat_log_path": "...",
  "last_runs": [...]
}
```

MCP tool can use `--json` output directly or call `status::run` logic via `core::*` extraction (Phase 1.7). The `StatusReport` struct is private to `src/cli/status.rs` — Phase 1.7 may need to make it public or move to `src/core/`. That is a Tầng 1 decision for Architect in P006/P007.

The macOS 15 launchctl format finding is durable: any future status-related phiếu (P006 MCP, Phase 2 Telegram alert for "next fire" display) should reference this Discovery for the descriptor-based approach.
