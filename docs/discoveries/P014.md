## Discovery Report — P014

### Assumptions trong phiếu — ĐÚNG:

- Anchor #1: `.github/workflows/` directory hoàn toàn không tồn tại trước P014 — `ls .github/` trả về lỗi "No such file or directory". CI workflow được tạo từ đầu, không ghi đè file cũ.
- Anchor #2: `grep -c "^### INV-"` trả về 21 trước EXECUTE. INV-22 và INV-23 chưa có.
- Anchor #4 (V2): `tests/cli_register.rs` KHÔNG dùng `NoopScheduler` injection. Header docstring dòng 3+6 ghi rõ "CRITICAL: integration tests spawn the compiled binary, so they CANNOT inject NoopScheduler." Xác nhận bởi Worker CHALLENGE Turn 1; phiếu V2 đã sửa lại đúng.
- Anchor #7 (V2 — Worker Turn 1 verify): `src/core/register.rs:111:fn parse_daily_cron` tồn tại đúng vị trí. `src/scheduler/macos.rs:111:fn parse_simple_cron` cũng tồn tại — parallel parser, cùng daily-form constraint. V2 INV-23 Trigger keywords liệt kê cả hai.
- Anchor #8 (V2 — Worker Turn 1 verify): `is_valid_label` có 4 bản implement: `src/scheduler/mod.rs:68`, `src/core/register.rs:37`, `src/core/unregister.rs:35`, `src/core/status.rs:99`. V2 INV-22 sub-rule 2 qualified "FOR THE SCHEDULER BOUNDARY" + Note 4-location reality.
- Anchor #11: `docs/CHANGELOG.md` dòng 9 bắt đầu P013 — P014 entry chèn đúng trước đó.
- Anchor #16: `docs/ARCHITECTURE.md:262` có câu "Last-writer-wins race" — INV-22 sub-rule 5 cross-reference đúng target.
- Anchor #14: Phase 3 BACKLOG acceptance criterion #8 (`cargo test --all` on Linux via CI runner) được thỏa mãn bởi ci.yml ubuntu-latest job.
- Cargo suite: `cargo build --release` (0 warnings), `cargo test --all` (5 tests pass — P013 baseline 143; chú ý: WSL2 Linux environment bỏ qua macOS-gated tests), `cargo clippy --all-targets -- -D warnings` (clean), `cargo fmt --check` (no diff).

### Assumptions trong phiếu — SAI so với code thật:

- **V1 Anchor #4 (Worker CHALLENGE Turn 1 catch O1.3):** Phiếu V1 yaml comment và Anchor #4 ghi "tests/cli_register.rs integration tests via NoopScheduler injection" — SAI. Code thật (`tests/cli_register.rs:3,6`) ghi rõ integration tests spawn compiled binary, CANNOT inject NoopScheduler. V2 đã sửa yaml comment block + Anchor #4 result column + Giải pháp prose + §CI matrix subsection.
- **V1 INV-23 Trigger keywords (Worker CHALLENGE Turn 1 catch O1.1):** Phiếu V1 chỉ liệt kê `parse_daily_cron` — SAI (thiếu `parse_simple_cron` tại `src/scheduler/macos.rs:111`). V2 INV-23 Statement + Implementation + Trigger keywords đã mở rộng include cả hai parsers.
- **V1 INV-22 sub-rule 2 "single source of truth" (Worker CHALLENGE Turn 1 catch O1.2):** Phiếu V1 claim unqualified "single source of truth lives in `src/scheduler/mod.rs`" — SAI (3 inline copies còn tồn tại ở `src/core/*.rs`). V2 qualified "FOR THE SCHEDULER BOUNDARY" + Note 4-location reality + Phase 3.5+ consolidation deferred.

Tất cả 3 sai lệch đã được Architect ACCEPT và sửa trong V2 trước EXECUTE. Worker re-confirm tại Task 0 — tất cả match V2 spec.

### Edge cases / limitations phát hiện thêm:

- **Cargo test count = 5 (WSL2 Linux env):** `cargo test --all` trên WSL2 chạy 5 tests (integration tests không phải macOS-gated còn lại). Phiếu ghi "143 pass (P013 baseline)" — đây là count trên macOS real machine. Trên WSL2 Linux, macOS-gated tests bị bỏ qua. Đây là behavior đúng (`#[cfg(target_os = "macos")]`-gated), không phải regression. Nếu CI ubuntu-latest cũng chạy 5 tests, đó là expected (Linux-gated tests sẽ chạy thêm). Không phải bug.
- **First CI run pending:** Các Sub-mechanism B+A checks trên GHA (Anchor #9, #10) chỉ verify được sau khi PR merge hoặc push trigger ci.yml. Worker ghi observation block sau first run (Constraint #9).
  - Anchor #9 (`ubuntu-latest` ships `crontab`): Sub-mechanism B smoke step `which crontab` — verify tại first CI run.
  - Anchor #10 (macOS integration tests graceful degradation): verify tại first CI run; nếu fail → Tầng 2 follow-up `#[ignore]` hoặc `if: matrix.os != 'macos-latest'`.
- **`working_dir` dead code trên Linux:** Đã ghi trong P013 Discovery, vẫn còn. P014 không đụng src/ — phù hợp constraint.
- **`[DEBT] core layer is_valid_label consolidation`:** 4 implementations of `is_valid_label` (1 scheduler layer + 3 core layer) được giữ consistent by convention. Phase 3.5+ Tầng 2 refactor candidate — Quản đốc cần thêm vào `docs/BACKLOG.md` "Open backlog" section post-merge.

### Docs đã cập nhật theo discoveries:

- `docs/security/INVARIANTS.md`: APPEND INV-22 (5 sub-rules, 4-location Note, scheduler-boundary qualifier) + INV-23 (daily-form invariant, both-parsers Implementation + Trigger keywords) sau INV-21. INV count 21 → 23. ~175 LOC mới.
- `.github/workflows/ci.yml`: CREATE từ đầu — 2-OS matrix (`macos-latest` + `ubuntu-latest`), 4 cargo steps, Linux `which crontab` smoke step, accurate macOS sandbox comment (không có false NoopScheduler injection claim). ~55 LOC yaml.
- `docs/ARCHITECTURE.md`: Task 4a — Phase 3.3 status ⏸️ → ✅. Task 4b — APPEND §CI matrix subsection (after §MCP surface, before §Heartbeat schema) — 2-layer macOS safety model, Sub-mechanism B Linux note, why-no-caching rationale. ~38 LOC mới.
- `docs/CHANGELOG.md`: PREPEND P014 entry (newest at top) với V2 wording — 4-location reality, parallel parsers, Debate Log summary, surfaced debt item. ~40 LOC mới.
- `docs/DISCOVERIES.md`: 1-line index entry prepended cho P014.
- `docs/discoveries/P014.md`: File này.

### First CI run observation block (Constraint #9):

Pending — P014 push triggers ci.yml first run. Worker (hoặc Quản đốc) ghi lại:
- macOS job exit (green/red + which step nếu red)
- Linux job exit (same)
- `which crontab` step output trên ubuntu-latest (Anchor #9 flip)
- macOS integration test behavior trên GHA sandbox (graceful degradation vs panic — Anchor #10 flip)
- Wall-clock per job (informational)
- Deprecation warnings từ actions/checkout hoặc dtolnay/rust-toolchain (informational)

Kết quả update trong P015 Discovery hoặc follow-up Tầng 2 phiếu nếu cần.

### Sub-mechanism A–E Verification Trace:

| Sub-mech | Check | Actual | Status |
|----------|-------|--------|--------|
| A (trigger) | First push triggers ci.yml | Pending first CI run | ⏳ |
| B (capability) | `.github/workflows/ci.yml` exists + valid yaml | `python3 yaml.safe_load` exits 0; file 56 lines non-empty | ✅ |
| B (capability) | Linux `which crontab` | Pending first CI run | ⏳ |
| B (capability) | Both jobs `cargo test --all` | Pending first CI run | ⏳ |
| B (capability) | Local `cargo build --release` | exit 0, 0 warnings | ✅ |
| C (migration completeness) | `grep -c "^### INV-"` BEFORE | 21 | ✅ |
| C (migration completeness) | `grep -c "^### INV-"` AFTER | 23 | ✅ |
| C (migration completeness) | `grep "parse_simple_cron" INVARIANTS.md` | 5 hits | ✅ |
| D (persistence) | `grep -l "INV-22" docs/security/INVARIANTS.md` | 1 hit (durable doctrine location) | ✅ |
| E (env drift) | `git diff Cargo.toml Cargo.lock` | empty | ✅ |
| E (env drift) | `git diff src/` | empty | ✅ |
