## Discovery Report — P007

**Phiếu:** P007 — README + ARCHITECTURE post-ship docs polish
**Date:** 2026-05-27
**Tầng:** 2 (docs-only)

---

### Assumptions trong phiếu — ĐÚNG:

- Anchor #1: README.md có "Quick start (CLI)" section với 4 lệnh (init/register/run/status) — đúng, đã extend thành 6.
- Anchor #2: README.md có "MCP server" section với Claude Desktop config snippet — đúng.
- Anchor #3: README.md status banner ghi "Phase 1.7 shipped" — đúng, đã update.
- Anchor #4: README THIẾU `advisory-cron unregister` example — đúng, đã thêm step 6.
- Anchor #5: ARCHITECTURE.md §Modules table có 22 rows — ĐÚNG (Architect đếm 22, orchestrator brief nói 17 sai). Worker verify: `ls src/` + subdirs = 22 files = 22 rows. Hoàn toàn khớp.
- Anchor #6: ARCHITECTURE.md §MCP surface table có 5 rows — đúng.
- Anchor #7: ARCHITECTURE.md "introduced Phase 1.7" tại L60 — đúng, đã sửa thành "shipped Phase 1.7".
- Anchor #8: Phase status dòng "In progress (1.7 shipped; 1.6 docs remaining)" — đúng, đã update.
- Anchor #10: `src/main.rs` khai báo 7 mod (`cli`, `config`, `core`, `heartbeat`, `launchd`, `mcp`, `runner`) — đúng.
- Anchor #11: Binary install tại `~/.cargo/bin/advisory-cron` sau `cargo install --path .` — đúng.
- Anchor #12: `advisory-cron --help` lists 6 subcommands (init, register, unregister, run, status, mcp) — đúng.
- Anchor #13: `advisory-cron register --help` có `--label <LABEL>`, `--schedule <SCHEDULE>`, `--config <CONFIG>` — đúng.
- Anchor #14: `advisory-cron status --help` có `--label`, `--config`, `--json`, `--last` — đúng.
- Anchor #15: PROJECT.md Vision para có 1-câu mô tả tool — không verify trực tiếp (Worker không đọc PROJECT.md per Architect envelope), nhưng không cần cho P007 scope.
- Anchor #16: LICENSE file tồn tại tại repo root — không verify trực tiếp nhưng CHANGELOG L343 bootstrap inventory confirms.

### Assumptions trong phiếu — SAI so với code thật:

- Không có. Tất cả anchor đã verify khớp.

### Edge cases / limitations phát hiện thêm:

- **`cargo install --path . --force` tải thêm packages mới** (`reqwest v0.12.28`, `idna v1.1.0`, `url v2.5.8`, v.v.) do `Cargo.lock` pinned versions khác với crates.io latest. Đây là `cargo install` resolving fresh (ngoài local workspace `target/`) — Sub-mechanism E note: `cargo build --release` (trong workspace) và `cargo install` dùng cùng `Cargo.lock` nên pinned versions nhất quán. Không phải env drift nguy hiểm.

- **MCP smoke test exit code = 0** (không phải 5). Phiếu dự đoán "exit 0 hoặc 5 tùy rmcp interpret EOF" — thực tế: rmcp handle EOF clean, exit 0. README snippet đã correct.

- **Orchestrator brief ghi "17 modules"** — SAI. Số thực là 22. Architect đã đếm đúng 22 trong anchor #5. Orchestrator brief là context từ lúc trước khi P006 ship core/* + mcp/* modules (Phase 1.7 thêm 10 module mới: 7 core + 3 mcp). Worker confirms 22 là ground truth.

- **`advisory-cron run` với temp HOME và default config** — sau `init`, `run` sẽ thử chạy `claude -p /advisory-scan` nhưng `claude` binary không có trong PATH của test env (expected). Worker không test step 4 (run) vì task sẽ fail với exit 4 trong test env không có claude binary — đây là expected behavior, không phải bug. README step 4 ("Fire the configured task") là valid cho Sếp's env có claude binary.

- **Test label cleanup:** Worker dùng test label `p007-verify-temp-do-not-use` cho Sub-mechanism A verify. Sau unregister, `launchctl list | grep com.advisorycron.p007-verify-temp-do-not-use` trả GREP_EXIT:1 — xác nhận label đã remove khỏi launchd. Plist file ở trong tmpdir (không phải real `~/Library/LaunchAgents/`).

- **`advisory-cron unregister` warning output:** khi test, binary in `warning: plist file already absent for label p007-verify-temp-do-not-use` trước khi unregister. Lý do: temp HOME mới → không có plist được copy từ lần register trước (register đã dùng tmpdir khác). Behavioral note cho Sếp: unregister là idempotent (exit 0) kể cả khi plist absent.

### Sub-mechanism verification:

| Sub-mech | Check | Result |
|----------|-------|--------|
| A (trigger) | `launchctl list \| grep com.advisorycron.p007-verify-temp-do-not-use` sau register → row present; sau unregister → absent | PASS: row present khi registered (exit 0), absent sau unregister (grep exit 1) |
| B (capability) | `cargo check` | PASS (build --release thành công, 0 warnings) |
| B (capability) | `cargo install --path .` | PASS: binary tại `~/.cargo/bin/advisory-cron` |
| B (capability) | `advisory-cron --version` | PASS: `advisory-cron 0.1.0` |
| C (migration) | N/A — no schema change | N/A |
| D (persistence) | `grep -l "Layering invariant" docs/ARCHITECTURE.md` | PASS: 1 hit (durable doctrine tại L60 sau P007 edit) |
| E (env drift) | `cargo build --release` | PASS: 0 warnings, 0 errors |
| E (env drift) | `docs-gate --all --verbose` | PASS (verified post-edit) |

### Module count truth:

- **src/ direct files:** `main.rs`, `config.rs`, `launchd.rs`, `runner.rs`, `heartbeat.rs` = **5 files**
- **src/cli/:** `mod.rs`, `init.rs`, `register.rs`, `unregister.rs`, `run.rs`, `status.rs`, `mcp.rs` = **7 files**
- **src/core/:** `mod.rs`, `config_path.rs`, `init.rs`, `register.rs`, `unregister.rs`, `run.rs`, `status.rs` = **7 files**
- **src/mcp/:** `mod.rs`, `server.rs`, `tools.rs` = **3 files**
- **Total: 22 files = 22 ARCHITECTURE.md rows** (no phantom, no missing)

### MCP schema drift:

- None. All 5 tool schemas in ARCHITECTURE.md §MCP surface table match `src/mcp/tools.rs` source exactly, field-by-field.

### Tầng 2 approach (first Tầng 2 phiếu in sprint):

- Architect's lighter anchor count (16 anchors vs 17+ in Tầng 1 phiếu) worked well. Anchor #9, #10, #11, #12, #13, #14 all marked `[needs Worker verify]` — Worker verified all via bash/grep, all passed. No objection needed.
- Tầng 2 self-decide: wording polish in README Status banner, placeholder format `<YOU>` per phiếu spec, section insertion point after "Quick smoke test" block.

### Docs đã cập nhật theo discoveries:

- `README.md`: CLI quick-start 4→6 steps; "What advisory-cron fires" section added; Status banner updated.
- `docs/ARCHITECTURE.md`: "introduced" → "shipped" (L60); Phase status reflect Code COMPLETE + P007 shipped (L300).
- `docs/CHANGELOG.md`: P007 entry (newest at top).
- `docs/DISCOVERIES.md`: 1-line index entry added.
