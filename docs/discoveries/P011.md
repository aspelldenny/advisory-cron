## Discovery Report — P011

### Assumptions trong phiếu — ĐÚNG:

- Anchor #3: `src/launchd.rs::generate_plist` có full allowlist check `label.chars().all(|c| c.is_ascii_alphanumeric() || c == '-' || c == '_')` tại line 51. INV-12 point 2 đúng như phiếu giả định.
- Anchor #5: `.git/hooks/pre-commit` line 137 chứa `grep -q "## .*$PHIEU_ID" docs/DISCOVERIES.md 2>/dev/null` — H2-only pattern, khớp đúng với phiếu.
- Anchor #6: `docs/DISCOVERIES.md` có cả H2 header (`## P<NNN>`) lẫn list-item (`- 2026-MM-DD P<NNN>:`) cho mỗi phiếu P001-P010. New hook regex giữ backwards-compat.
- Anchor #7: INVARIANTS.md INV-12 spec (line 137-153) ghi 2 enforcement point. Line 147 prose references `cli/register.rs::run_with_deps` (pre-P006 location) — stale nhưng spec unchanged.
- Anchor #8: INVARIANTS.md INV-18 (MCP boundary `validate_label`) tách biệt — không thuộc P011 scope. Confirmed.
- Anchor #10: Baseline 141 tests confirmed. P011 → 144 (+3 named attack-class tests).
- Anchor #11: Không cần dep mới. Predicate dùng `std::char::is_ascii_alphanumeric` only.
- Anchor #12: `docs/BACKLOG.md` "Open backlog" có đúng 3 debt items. 2 items đã được move sang "Recently shipped".
- Anchor #13: `docs/BACKLOG.md` "Recently shipped" section trước là `(empty until Phase 1 ships)` — đã được populate.
- Anchor #14: `.phieu-counter` = `011`. Confirmed.
- Anchor #15: Hook dùng `set -uo pipefail` (line 12), `grep -q` tại line 137. Đã đổi sang `grep -Eq`.
- Anchor #16: `git config --get core.hooksPath` = unset → hook tại `.git/hooks/pre-commit` là active hook. Confirmed.

### Assumptions trong phiếu — SAI so với code thật:

- **Anchor #1 (SẾCH):** Phiếu và Sếp brief giả định `src/core/register.rs::run` pre-flight chỉ có `args.label.is_empty()` check. Thực tế: code đã có `pub fn is_valid_label(label: &str) -> bool` helper + full allowlist check `if !is_valid_label(&args.label)` tại lines 34-53. Task 1 là NO-OP. BACKLOG.md debt item #1 (INV-12 label sanitization 2-point enforcement) là STALE — enforcement đã được implement trong một sprint prior mà không update BACKLOG.

- **Anchor #2 (STALE):** Phiếu đặt câu hỏi về `src/core/unregister.rs::run` pre-flight shape (unknown, có thể empty-check-only). Thực tế: code đã có `pub fn is_valid_label` helper + full allowlist check `if !is_valid_label(&args.label)` tại lines 32-48. Task 2 là NO-OP. Cả 2 files đều đã có INV-12 enforcement trước khi P011 bắt đầu.

- **Anchor #4 (PARTIAL STALE):** Phiếu đặt câu hỏi liệu `is_valid_label` có tồn tại trong `launchd.rs` hay không. Thực tế: helper không có trong `launchd.rs` — nó đã được define trực tiếp trong `src/core/register.rs` (line 34), `src/core/unregister.rs` (line 32), và `src/core/status.rs` (line 92) — mỗi module có copy riêng. `src/mcp/tools.rs` có `validate_label` function riêng (hơi khác signature). Không có shared helper — 3 copies với cùng predicate.

- **Anchor #9 (STALE in implication):** Phiếu hỏi xem có existing tests dùng `NoopLaunchctl` không. Thực tế: YES, và tests cho invalid-label rejection đã tồn tại trong cả 2 files (`run_rejects_invalid_label` trong register.rs và unregister.rs). P011 chỉ thêm 3 named tests cho specific attack classes (whitespace, path separator, shell metachar) với explicit assertion về `bootstrap_calls.len() == 0`.

### Edge cases / limitations phát hiện thêm:

- **Duplication của `is_valid_label`:** Function `is_valid_label` được define độc lập trong `src/core/register.rs`, `src/core/unregister.rs`, và `src/core/status.rs` với cùng predicate. `src/mcp/tools.rs` có `validate_label` với slightly different signature. Đây là code duplication nhỏ — không phải bug (all predicates đồng nhất), nhưng là technical debt. Future: có thể extract ra `src/core/mod.rs` shared helper. Không thuộc P011 scope — ghi vào BACKLOG hoặc future phiếu.

- **INVARIANTS.md INV-12 line 147 stale prose location:** References `src/cli/register.rs::run_with_deps` (pre-P006 location). Post-P006 location là `src/core/register.rs::run`. Worker LEFT THIS AS-IS (Tầng 2 freebie — trong phạm vi Architect để Worker tự quyết). Reason: INV-12 scope changes are noted in INVARIANTS.md ownership header; updating prose location would touch `docs/security/INVARIANTS.md`, which the phiếu explicitly marks as OPTIONAL. Leaving as known stale prose — log for next Tầng 1 INVARIANTS phiếu.

- **Hook dry-run limitations:** Pre-commit hook chỉ check khi có NEW phiếu file staged (`--diff-filter=A`). Nếu chỉ stage `docs/DISCOVERIES.md` mà không stage new phiếu file, section 3c không trigger. Đây là existing behavior, không phải P011 regression.

### Docs đã cập nhật theo discoveries:

- `docs/CHANGELOG.md`: P011 entry prepended trên P010 entry.
- `docs/BACKLOG.md`: 2 debt items moved từ "Open backlog" → "Recently shipped". Sprint P001-P010 summary line added. Item 3 (`fire_task` no process timeout) ở lại "Open backlog".
- `docs/DISCOVERIES.md`: P011 list-item index entry appended (newest at top) theo CLAUDE.md doctrine format.
- `docs/security/INVARIANTS.md`: KHÔNG sửa (INV-12 stale prose location left as-is, logged above).

### Task 2 decision:
Task 2 SKIPPED — `src/core/unregister.rs::run` đã có full allowlist check trước P011. No new tests needed (existing `run_rejects_invalid_label` test đã cover).

### Hook regex validation:
- P001-P010: 10/10 OK (cả H2 form lẫn list-item form đều match).
- P999: correctly rejected (no false positive).
- bash syntax check: `bash -n .git/hooks/pre-commit` → exit 0.

### is_valid_label helper reuse decision:
Không reuse existing helpers (each module đã có private copy). P011 chỉ thêm tests, không thêm implementation.

### Final test count:
- Baseline: 141 tests
- Added: 3 tests (`register_rejects_label_with_whitespace`, `register_rejects_label_with_path_separator`, `register_rejects_label_with_shell_metachar`)
- Final: 144 tests
